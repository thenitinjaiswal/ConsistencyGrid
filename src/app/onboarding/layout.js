export const metadata = {
  title: "Onboarding - ConsistencyGrid",
  description: "Complete your ConsistencyGrid setup in 4 easy steps",
};

export default function OnboardingLayout({ children }) {
  return <>{children}</>;
}
