export const metadata = {
  title: "Settings",
  description: "Manage your ConsistencyGrid account settings, wallpaper preferences, and notification preferences.",
};

export default function SettingsLayout({ children }) {
  return children;
}
