export const metadata = {
  title: "Sign Up",
  description: "Create a free ConsistencyGrid account to generate your personalized life calendar wallpaper.",
};

export default function SignupLayout({ children }) {
  return children;
}
