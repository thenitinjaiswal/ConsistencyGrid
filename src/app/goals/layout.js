export const metadata = {
  title: "Goals",
  description: "Set challenges and track progress on your wallpaper with goal tracking.",
};

export default function GoalsLayout({ children }) {
  return children;
}
