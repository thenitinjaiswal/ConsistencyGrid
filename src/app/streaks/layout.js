export const metadata = {
  title: "Streaks",
  description: "Track your consistency, view your current streak, and unlock milestones as you build momentum.",
};

export default function StreaksLayout({ children }) {
  return children;
}
