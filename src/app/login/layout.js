export const metadata = {
  title: "Log In",
  description: "Log in to ConsistencyGrid to access your life calendar wallpaper and track your habits.",
};

export default function LoginLayout({ children }) {
  return children;
}
