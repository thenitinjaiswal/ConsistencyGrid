export const metadata = {
  title: "Pricing",
  description: "Choose the perfect plan for your life calendar tracking. Start free and upgrade when you're ready.",
};

export default function PricingLayout({ children }) {
  return children;
}
