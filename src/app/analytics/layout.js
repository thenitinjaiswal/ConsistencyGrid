export const metadata = {
  title: "Analytics",
  description: "View insights and trends about your habit performance and completion rates.",
};

export default function AnalyticsLayout({ children }) {
  return children;
}
